# 任务中心验收记录

日期：2026-09-07。范围：部署机上的独立测试环境；以下运行状态为验收时快照，不代表当前状态。后续生产切换已完成，部署机本地 `docs/production-deployment.md` 保存运维记录，不纳入公开仓库。

## 已交付

- 独立工作台：列表、搜索、状态筛选、分页、任务链接、阶段/耗时、处理记录。
- URL 和文件提交；SQLite 持久化；串行队列；幂等请求；失败/中断手动重试。
- 历史文稿、原始转录、摘要、翻译阅读；复制；MD/TXT 下载。
- SSE 重连与每 5 秒查询兜底。只展示真实阶段，不生成模拟百分比。
- worker 独立进程，模型/同步请求不阻塞 API；执行器离线提示。
- 历史任务迁移工具、上游适配说明、独立部署文件和 AGENTS.md。

## 自动化

最终镜像运行 `python3 -m unittest discover -s tests/task_center -v`：12/12 通过。

覆盖：提交持久化/幂等、并发领取只产生一个 running、队列保留/中断恢复/重试、导出、重复上传、输入/跨源/路径校验、筛选分页、无损幂等历史迁移、静态页/CSP、上游 adapter 契约和空语言解析、执行器离线、失败保留已生成原文。

`git diff --check` 通过。现有上游文件变更集中在 4 个 backend 文件，共 21 行新增、8 行删除（新增独立模块不包含在此数字内）。原 static/app.js、static/index.html、Dockerfile、docker-compose.yml 未修改。

## 真实链路

1. 旧任务 `87581bed-5bfd-421f-814d-4cfe65f668d1` 导入成功：1 条、缺失文件 0；原 JSON 保留并另存备份。
2. 合成的 12.224 秒音频任务 `26c469a4-2398-4930-bee1-1626369fe4c1`：真实 large-v3/CUDA 转录 + CPA 模型整理/摘要成功，23:41:01 至 23:41:28。
3. 排在后面的文本任务 `7e6cb8e9-2f87-482c-bb5e-053e6c86c36d`：23:41:28 才开始，23:41:45 完成，证明无并行抢占。
4. 执行过程中列表 API 一次测量约 0.0024 秒（仅样本，不是性能保证）。
5. 中断测试：仅对测试 worker 执行 `docker restart -t 0`；任务 `c4498953-a793-4f46-813c-0b850181c0d7` 从 running 变 interrupted，原始文稿仍可读取。排队任务 `69766974-355b-4890-bf63-86623c4835e8` 继续执行。
6. 通过网页点击“重新排队”创建 `2357b0c6-87db-43b6-bfbd-733e6b6d788b`，最终 completed，保留 retry_of 和原中断记录。
7. 最终构建通过网页文件选择器上传 sample.txt，刷新后仍能显示任务 `9c43615c-e5cd-4582-acf9-e9d56f518e2a`，最终 completed。空语言解析修正后不再发生无意义的中文到中文翻译。

## 浏览器验收

- 历史任务通过 URL 直接打开，刷新恢复；结果页能切换原稿、整理稿、摘要、翻译。
- 实际点击 TXT 下载，浏览器收到并保存中文文件名的附件。
- Playwright 将 SSE 端点模拟为 503，页面显示“定时同步中”，仍正确查询中断任务与后台队列；撤除模拟后恢复实时连接。
- 1440×1000 桌面与 390×844 窄屏截图检查通过；窄屏无横向溢出。
- 浅色和深色均检查；安全 Markdown 输入测试未产生 script/img 元素。
- 恢复正常网络后浏览器检查到 0 errors / 0 warnings。测试期间人为 503 和重建服务产生的连接错误是预期故障注入。

## 限制 / 未验收项

- 一条真实 B 站 URL 测试返回来源站 HTTP 412，任务正确标记 failed，后续队列继续。没有将这条 URL 记为端到端成功；来源站风控需单独诊断。
- 本轮隔离验收未覆盖公网 Access/大文件上传；后续上线已验证 Access 及结果下载，大文件上传仍需专项验收。
- 尚未实测整机重启、长时间稳定性、海量历史数据、多用户隔离和干净环境重建。
- 历史创建时间上游没有保存，显示“历史导入”；不伪造原始时间。
- Markdown 采用安全子集，不执行内嵌 HTML/图片/链接；复杂表格可能按原文展示。
- 原接口保留，但不接入新队列；避免旧入口和新工作台同时提交 GPU 工作。

## 验收时的位置与运行状态（历史快照）

- 代码：`/home/kai/AI-Video-Transcriber-fork`，未 commit、未 push。
- 测试 Web：`127.0.0.1:18000`，容器 `transcriber-workbench-web-1` healthy。
- 测试 worker：`transcriber-workbench-worker-1`。
- 测试数据：`/home/kai/.local/share/transcriber-workbench-test`；包含验收任务，不直接用作生产库。
- 原容器 `ai-video-transcriber` 仍 healthy、首页 HTTP 200。
- 原 SSH Tunnel PID 26203、启动时间 2026-09-07 07:16:15 CST 未变。

上线前请按 `docs/task-center.md` 做最终迁移和路由切换；原部署与 SSH 不自动修改。
