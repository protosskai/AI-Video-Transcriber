# Fork 开发约定

## 项目背景与范围

- 用户独立维护的 Fork：`protosskai/AI-Video-Transcriber`。
- 上游：`wendy7756/AI-Video-Transcriber`。会定期同步上游，不计划将私有定制回贡献上游。
- 部署机开发目录：`/home/kai/AI-Video-Transcriber-fork`。
- 原运行目录 `/home/kai/AI-Video-Transcriber` 存在 Docker、GPU 和后端适配修改，不能覆盖、删除或假设这些修改已经迁入 Fork。
- 生产服务、已有任务结果和 SSH Tunnel 必须保留。代码变更不等于获准重启、切换部署、推送或执行破坏性迁移。

## 核心原则：以独立增强降低上游合并冲突

1. 优先新增独立模块、路由、静态资源和配置覆盖文件，避免重写上游主文件。前后端均需明确增强层与上游边界。
2. 尽量复用上游下载、转录、翻译、摘要能力；通过窄适配层访问，禁止复制整套上游流水线形成第二套实现。
3. 不用 monkey patch、隐式全局状态替换或复制大量代码来伪装“零修改”。无法安全解耦时，允许小而明确的上游接入点，并记录原因、契约和兼容性测试。
4. 保留现有 CLI、MCP 和原始 Web 使用方式。新增 API 使用独立命名空间；新工作台使用独立前端入口。任何改变旧接口行为的方案必须明确列出并验证。
5. 后端区分任务存储、调度、流水线适配、结果导出与 HTTP 接口。前端区分任务列表、详情、连接恢复和 API 客户端。
6. 不引入没有必要的微服务或中间件。单机首版优先 SQLite 和单任务执行器；真实并发需求出现后再扩展。
7. 独立维护增强依赖、Docker/Compose 覆盖配置和上游兼容说明，避免无关格式化及全仓重构。上游同步后运行契约与回归测试。

## 已确认的用户需求

- 前端可感知任务队列，查看每个任务的执行状态、阶段与结果。
- 刷新、关闭页面或更换客户端后，可重新查看服务器端任务历史与结果。
- 结果适合阅读，提供转录文稿、摘要、翻译的明确区分，并可下载文案。
- 功能应在统一前端体验中自然融合，不强制拆成多页面。

## 实现约束与验收方向

- 服务器持久化数据是任务状态的事实来源，不能仅依赖浏览器 localStorage 或 SSE 连接。
- 将任务状态与执行阶段分离；不伪造精确进度、剩余时间或断点恢复能力。
- 浏览器断开不得取消后台任务；SSE 需重连及查询兜底。耗时推理、模型加载与同步 LLM 调用不得阻塞 Web 事件循环。
- 默认限制 GPU 任务并发；队列领取要避免重复执行。服务重启后保留排队任务，执行中任务应明确标记中断，不能永久显示运行中。
- 保留历史文案，媒体缓存与任务记录分开管理。迁移现有 tasks.json/结果时先备份，支持幂等并报告缺失文件，禁止删除原数据。
- 下载链接默认仍需认证，不生成匿名公网分享链接。校验结果访问权限与文件路径；Markdown 渲染要防 XSS。
- API 密钥等秘密不得进入 Git、任务响应或普通日志；持久化任务优先保存凭据引用，而非明文密钥。
- 当前用户是个人使用；若未来开放多用户，必须增加任务所有权隔离，不能直接共享全量历史。
- 验证刷新恢复、多任务排队、SSE 断线、结果预览与下载、失败可见性、重启中断处理、旧接口兼容及上游同步。

## 产品定位与交互约定

- 核心是音视频转文字，首页优先贴链接/上传与开始转录；历史管理是辅助能力，不做内容管理后台。
- 默认阅读布局即应舒适，不堆功能按钮、解释性副标题、专注模式等额外概念。
- 复制原位反馈；成功提示自动消失；错误就地保留。改名原位编辑，禁止浏览器 prompt/confirm。
- 参考 DESIGN.md 的暖白/陶土色视觉与渐进呈现原则。当前前端入口是 `transcribe.html/css/js`；studio 文件是前轮验收稿，不再作为产品入口。

## 内容工作台增强约定

- 产品范围见 `docs/product-v2.md`，视觉与交互规范见 `DESIGN.md`。当前实现第一阶段；不得将路线图功能描述为已经完成。
- `contents/content_jobs` 是独立增强层；保留原 `jobs/events`、历史结果、旧任务链接与上游流水线。
- 重试创建新的执行记录，不覆盖旧成果。提交必须幂等；取消排队须与 worker 领取互斥，不能伪装支持取消运行中任务。
- 归档、回收站只更改内容元数据，不能物理删除原文或媒体。下载继续受 Access 保护。
- 每次发布前执行 task_center 全部测试与浏览器验收；测试库必须独立于生产库。本轮未授权新的 commit/push。

## 历史授权记录（不可当作本轮授权）

用户已批准实现、测试、替换生产部署及本次 commit/push。生产已切换至独立工作台，旧容器和测试容器停止并保留回滚；禁止误用测试库覆盖生产库。部署机专用参数 `deploy/production.env` 和运维记录 `docs/production-deployment.md` 仅保留在部署机，不进入公开仓库。后续运维先读取该本地记录；其他机器参考 `deploy/production.env.example` 与 `docs/task-center.md`。本次 Git 授权不代表授权后续部署变更，SSH Tunnel 不动。
